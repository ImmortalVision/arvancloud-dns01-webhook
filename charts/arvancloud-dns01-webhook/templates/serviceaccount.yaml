{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arvancloud-dns01-webhook.serviceAccountName" . }}
  namespace: {{ include "arvancloud-dns01-webhook.namespace" . }}
  labels:
    {{- include "arvancloud-dns01-webhook.labels" . | nindent 4 }}
{{- end }}
